"""Shared service utilities — MIGRATION SHIM.

All modules have moved to ``platform.*``. This package re-exports them
for backward compatibility.  New code should import from ``platform`` directly.
"""
