# Local MQTT for Hummingbot headless (conf_client.yml → mqtt_host: mosquitto).
# Internal LAN only; bind published to 127.0.0.1 via compose ports by default.

persistence false

listener 1883 0.0.0.0
allow_anonymous true
