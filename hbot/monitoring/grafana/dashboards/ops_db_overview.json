{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "Blotter (Last 100 Fills)",
      "type": "table",
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 0 },
      "datasource": { "type": "postgres", "uid": "postgres-ops" },
      "targets": [
        {
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT ts_utc, bot, variant, side, price, amount, fee_paid_quote, source_path FROM fills ORDER BY ts_utc DESC NULLS LAST LIMIT 100;",
          "refId": "A"
        }
      ]
    },
    {
      "title": "Wallet Equity History (Postgres)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 10 },
      "datasource": { "type": "postgres", "uid": "postgres-ops" },
      "targets": [
        {
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "SELECT ts_utc AS \"time\", equity_quote AS value, bot AS metric FROM exchange_snapshot WHERE $__timeFilter(ts_utc) ORDER BY ts_utc;",
          "refId": "A"
        }
      ],
      "fieldConfig": { "defaults": { "unit": "currencyUSD" }, "overrides": [] }
    },
    {
      "title": "Drawdown Curve (Postgres)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 10 },
      "datasource": { "type": "postgres", "uid": "postgres-ops" },
      "targets": [
        {
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "SELECT ts_utc AS \"time\", drawdown_pct * 100.0 AS value, bot AS metric FROM bot_snapshot_minute WHERE $__timeFilter(ts_utc) ORDER BY ts_utc;",
          "refId": "A"
        }
      ],
      "fieldConfig": { "defaults": { "unit": "percent" }, "overrides": [] }
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["hbot", "postgres", "ops-db"],
  "templating": { "list": [] },
  "time": { "from": "now-24h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Trading Desk Ops DB Overview",
  "uid": "hbot-ops-db-overview",
  "version": 1
}
