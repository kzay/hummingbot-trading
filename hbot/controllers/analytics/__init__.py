"""Analytics helpers (performance metrics, evaluation utilities).

This package intentionally keeps runtime dependencies minimal so controllers can
use it inside the live/paper event loop without pulling in heavy stacks.
"""

