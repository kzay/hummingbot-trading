# Archived / removed one-off scripts

- `hbot/_move_connector_patches.py` (removed): one-time helper to extract connector patch helpers from `hb_bridge.py` into `controllers/paper_engine_v2/connector_patches.py`. Do not resurrect unless a similar mechanical extraction is needed.
