strategy_class: ""
strategy_config:
  adapter_mode: "directional_mm"
  ema_fast: 8
  ema_slow: 21
  atr_period: 14
  base_spread: "0.0015"
  max_spread: "0.0050"
  # Strong trend bias
  trend_skew: "0.70"
  base_size_pct: "0.05"
  trend_size_mult: "2.0"
  contra_size_mult: "0.2"
  max_inventory_pct: "0.40"
  inventory_skew_factor: "2.5"
  levels: 2
  level_spacing: "0.60"
  max_daily_loss_pct: "0.03"
  max_drawdown_pct: "0.05"
  vol_spread_mult: "1.0"
  min_warmup_bars: 30

data_source:
  exchange: bitget
  pair: BTC-USDT
  resolution: 1m
  instrument_type: perp
  start_date: "2025-01-01"
  end_date: "2025-01-31"
  catalog_dir: data/historical

initial_equity: 500
fill_model: latency_aware
seed: 42
leverage: 1
step_interval_s: 60
warmup_bars: 60

synthesis:
  base_spread_bps: "5.0"
  vol_spread_mult: "1.0"
  depth_levels: 10
  depth_decay: "0.70"
  base_depth_size: "0.5"
  steps_per_bar: 4

output_dir: reports/backtest
