# ============================================================================
#  V2 Script Configuration — PMM + RSI + LLM
# ============================================================================
#  Usage:
#    start --script v2_with_controllers.py --conf v2_pmm_rsi_llm.yml
# ============================================================================

script_file_name: v2_with_controllers.py

# Markets and candles are auto-populated from the controller configs below.
# Only specify here if you need ADDITIONAL markets/candles beyond the controllers.
markets: ""
candles_config: ""

# Reference the controller config YAML(s) in conf/controllers/
controllers_config:
  - pmm_rsi_llm_1.yml

# ---- Global Drawdown Protection ----
# Set to null to disable, or a quote amount (e.g., 100.0 for $100 max loss)
max_global_drawdown_quote: null
max_controller_drawdown_quote: null
